<nav class="question-nav not-zoomable">
  <%= render 'shared/user_element_admin' %>
  <div class="mobile-content wide">
    <h2><%= Setup.first.name %> - administrácia</h2>
    <a href="<%=url_for administration_path %>"><i class="nav-right-icons icon-level-up"></i></a>
    <a class="user-element-cta"><i class="nav-right-icons icon-menu"></i></a>
  </div>
</nav>
<hr class="nav-offset not-zoomable">
<div id="header-panel" class="admin">

  <div class="buttons">
    <%= link_to "+ "+t('.label.add_user'), new_administrations_user_path, class: 'btn' %>
  </div>

  <div class="tools">
  </div>

</div>
<div id="questions-list" class="admin">
  <% @users.each do |user| %>
      <div id="user" class="question row">
        <div class="question-tools">
          <%= link_to('<i class="icon-edit"></i>'.html_safe, edit_administrations_user_path(user),  title: t('.tooltip.edit'), data: { toggle: :tooltip }) %>
        </div>
        <div class="question-icon"><i data-toggle="tooltip" title="<%= t '.tooltip.'+user.type.downcase %>" class="icon-user"></i></div>
        <div class="question-headline">
          <div class="title"><%= link_to user.login, edit_administrations_user_path(user) %></div>
          <div class="text"><%= user.first_name + " " + user.last_name %></div>
        </div>
      </div>
  <% end %>
</div>