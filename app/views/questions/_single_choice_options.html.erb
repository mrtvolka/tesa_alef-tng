<% @answers.each do |a| %>
    <label><input type="radio" name="answer" value="<%= a.id %>" class="answer-input">
      <i class="icon-radio answer-icon"></i>
      <div class="answer-text"><%= mathjax_tag %><%= a.answer_text %></div></label>
<% end %>